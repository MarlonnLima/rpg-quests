import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        backgroundColor: "#262626",
        borderColor: "#0D0D0D",
        borderWidth: 1,
        padding: 16,
        color: "#FFF",
        borderRadius: 8,
        flex: 1
    }
})